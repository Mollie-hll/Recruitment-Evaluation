·论文标题：Attention Is All You Need
·核心思想：抛弃传统卷积和循环模式，在注意力机制上建立一个全新的、更加高效、更易训练、泛化能力更强的基础模型。
·我的思考：*Self-Attention将单个序列的不同位置关联起来以计算序列表示，而RNN采取对齐序列的方式。也就是说，Self-Attention对序列中每个位置的计算结果都不依赖于其他位置的计算结果，而RNN则是严格串行的。没有这种顺序依赖，使得它能通过矩阵运算实现并行化。
           *多头注意力是transformer的核心创新，能让模型具备观察前后文的能力。
           *掩码机制：强迫模型学习而不是照搬，训练出模型生成的能力。
